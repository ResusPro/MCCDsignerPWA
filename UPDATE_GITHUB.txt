MCCDSigner PWA v0.2b — GitHub update

This build fixes the v0.2a deployment/cache packaging error.

Using GitHub Desktop:
1. Repository > Show in Explorer.
2. Copy ALL CONTENTS of this DEPLOY folder into the repository root.
3. Replace files when prompted.
4. Commit to main, then Push origin.

The new page must load assets/app-v0.2b.js and assets/app-v0.2b.css.
Old assets/app.js and assets/app.css may remain in the repository but are no longer used.

After GitHub Pages deploys, open the site in a normal browser tab while online.
The header and footer must both say v0.2b.
